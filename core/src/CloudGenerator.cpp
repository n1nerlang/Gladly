#include "CloudGenerator.hpp"

void CloudGenerator::executeCloudPass(int chunkX, int chunkZ, BlockID* chunkBuffer, CloudStyle style, float timeOffset) {
    // Stub implementation: does nothing for now
    (void)chunkX;
    (void)chunkZ;
    (void)chunkBuffer;
    (void)style;
    (void)timeOffset;
}
